import React from 'react';
import TooltipContainer from './TooltipContainer';
import Text from '../shared/Text.js';

export default () => {
  return (
    <TooltipContainer>
      <Text>
        This is the tooltip component which will help giving the users more details about one or more features.
      </Text>
    </TooltipContainer>
  );
}
